package io.jeffrey.world.things;

import org.junit.Test;

import io.jeffrey.world.WorldTestFramework;

public class TestTImage extends WorldTestFramework  {
  @Test
  public void construct() {
    
    // does not work because graphics isn't initialized yet... no good, yo
    // TImage thing = new TImage(makeSimpleContainer(), emptyData()); 
  }
}
