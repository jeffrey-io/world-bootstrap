package io.jeffrey.world.math;

public class EdgeMath {

    /**
     * does the given point touch the line formed between the points v0 and v1
     *
     * @param pnt
     * @param v0
     * @param v1
     * @return
     */
    /*
     * public static Vector2 computeMinimalLinePoint(final Vector2 pnt, final Vector2 v0, final Vector2 v1) { final double a = v1.x - v0.x; final double b = v1.y - v0.y; final double u = pnt.x - v0.x; final double v = pnt.y - v0.y; double lambda = a * u + v * b; lambda /= a * a + b * b; if (lambda < 0) { return null; } if (lambda > 1) { return null; } return new Vector2(a * lambda + v0.x, b * lambda + v0.y); }
     */
}
